'use strict';

function Util() {
  this.suitePath = null;
}

Util.prototype.syncNemoConfig = function(suitePath) {
  //read locator JSON files and sync with <suitePath>/config/nemo.json (setup.view.['view1', 'view2', 'view3']
};

module.exports = new Util();